# enable color support of ls and also add handy aliases
alias ls='ls -G'
alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'
alias grep='grep --color'

# Prompt to delete
alias rm='rm -i'
alias mv='mv -i'
alias cp='cp -i'

alias ..='cd ..'

export EDITOR="vim"

